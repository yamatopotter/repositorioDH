server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yamatopotter/configs/
  zipkin:
    base-url: http://${ZIPKIN_URL:localhost}:9411